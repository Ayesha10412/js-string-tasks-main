// ---

// ### Task-3: 

// Check whether a string contains all the vowels `a`, `e`, `i`, `o`, `u` 

// ---


function nameCount(name){
    for(const char of name){
        if(char==='a' || char==='e' || char==='i' || char==='o'|| char==='u'){
return true;          
        }
     else {
return false;        }
    }
    return count;
}
   

console.log(nameCount('ghtdks'));